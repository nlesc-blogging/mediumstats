---
title: "Blog summary"
author: "Pablo Rodríguez"
date: '2022-07-22'
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(mediumstats)
```


```{r rss}
# Build data frame
get_feed(destfile = '../data/temp.xml') |> 
  get_blogs_set() |> 
  blogs_set_as_df() -> df

print(df)
```

```{r stats}
mes <- get_mes_stats('../data/mes.csv') # With Medium enhanced stats
msg <- get_msg_stats('../data/msg.csv') # With Medium stats grabber
```

```{r merge-mes}
merge(df, mes, by = 'id', all = TRUE)
```
```{r merge-msg}
merge(df, msg, by = 'id', all = TRUE)
```
